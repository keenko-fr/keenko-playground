# Keenko Playbook overrides

List only deliberate project-specific deviations from the pinned Keenko Playbook.

For each override, state the shared default, the project decision, and the reason. Remove an override when the project realigns with the shared convention.
